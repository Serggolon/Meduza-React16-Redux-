import styled from 'styled-components';
import Container from './container'

const ContactsWorkingHours = styled(Container)`
display: grid;
grid-area: a;
height: auto;
align-items: center;
justify-items: left;
`;

export default ContactsWorkingHours;