import styled from 'styled-components';
import Container from './container'

const ContactsPhoneNumbers = styled(Container)`
display: grid;
grid-area: b;
align-items: top;
justify-items: left;
`;

export default ContactsPhoneNumbers;